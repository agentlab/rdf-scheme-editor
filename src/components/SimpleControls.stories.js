import React from 'react';
import { storiesOf } from '@storybook/react';

import { Table } from 'antd';

  storiesOf('Редактор атрибутов классов', module)
    .add('Без данных', () => <div />)
    .add('С корректными данными', () => <div />);
  